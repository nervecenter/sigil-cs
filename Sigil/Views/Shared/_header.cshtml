@{ 
    string action = ViewContext.RouteData.Values["action"].ToString();
    string controller = ViewContext.RouteData.Values["controller"].ToString();
    bool transparent = false;
    if (controller == "Home" && (action == "LandingPage"))
    {
        transparent = true;
    }
}


<nav class="navbar navbar-fixed-top @(!transparent ? "navbar-default" : "") @(transparent ? "navbar-transparent" : "")">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a href="@Url.Action("Index", "Home")"><img src="/Content/Images/symbol-small.png" class="navbar-logo"></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul id="navbar-list" class="nav navbar-nav navbar-right">
                @using (Html.BeginRouteForm("Search Page", FormMethod.Post, new { @class = "navbar-form navbar-left", role = "search", style="width:25vw;" }))
                {
                    <div class="form-group" style="width:100%;">
                        <input id="site-search-box" type="text" value="@Request.Form["searchTerm"]" data-provide="typeahead" name="searchTerm" class="form-control typeahead" placeholder="Search for a company, person, or product" style="width:100%;">
                    </div>
                }
                @{Html.RenderAction("_LoginPartial", "Shared");}
                
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>